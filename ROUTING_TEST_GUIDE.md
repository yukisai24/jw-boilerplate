# 🧪 라우팅 권한 & 라이센스 테스트 가이드

## 📋 개요

이 테스트 페이지는 새로 구축된 통합 라우팅 시스템의 권한 및 라이센스 기능을
실시간으로 테스트할 수 있는 종합적인 도구입니다.

## 🚀 접근 방법

### 1. 직접 URL 접근

```
http://localhost:3000/test/routing
```

### 2. 홈페이지에서 접근

1. 메인 페이지(`/`) 방문
2. "🚀 라우팅 테스트 페이지 이동" 버튼 클릭

## 🎯 주요 기능

### 1. **사용자 설정 패널** 👤

- **인증 상태 토글**: 로그인/로그아웃 상태 변경
- **사용자 역할 선택**: 5가지 역할 중 선택
  - `GUEST` - 게스트 사용자
  - `USER` - 일반 사용자
  - `MANAGER` - 관리자
  - `ADMIN` - 시스템 관리자
  - `SUPER_ADMIN` - 최고 관리자
- **라이센스 타입 선택**: 4가지 라이센스 중 선택
  - `FREE` - 무료 (기본 기능)
  - `BASIC` - 기본 (표준 내보내기)
  - `PREMIUM` - 프리미엄 (고급 분석, API 접근)
  - `ENTERPRISE` - 엔터프라이즈 (모든 기능)

### 2. **라우트 접근 권한 현황** 🛣️

- 모든 라우트의 접근 권한을 실시간으로 표시
- 색상 코딩:
  - 🟢 **초록색**: 접근 가능
  - 🔴 **빨간색**: 접근 불가
- 각 라우트별 카테고리 표시 (auth, dashboard, admin, license, public)
- **테스트 버튼**:
  - "이동": 권한이 있는 경우 정상 이동
  - "강제 시도": 권한이 없는 경우 무단 접근 시도

### 3. **동적 네비게이션 미리보기** 🧭

- 현재 사용자 권한으로 생성되는 네비게이션 메뉴 미리보기
- 카테고리별 그룹화
- 실제 네비게이션에서 표시될 항목들 확인

### 4. **권한 요구사항 상세** 📋

- 각 라우트별 세부 권한 요구사항
- 필요한 역할, 라이센스, 기능 목록 표시
- AND/OR 조건 표시

### 5. **테스트 시나리오** 🎯

원클릭으로 다양한 시나리오 테스트:

- **시나리오 1**: 게스트 사용자 (최소 권한)
- **시나리오 2**: 무료 사용자 (기본 권한)
- **시나리오 3**: 프리미엄 매니저 (중간 권한)
- **시나리오 4**: 엔터프라이즈 관리자 (최대 권한)

### 6. **접근 권한 통계** 📊

- 접근 가능한 라우트 수
- 접근 불가능한 라우트 수
- 네비게이션 카테고리 수
- 총 라우트 수

## 🔍 테스트 시나리오 예시

### 시나리오 1: 무료 사용자 테스트

1. 인증 상태를 "로그인됨"으로 설정
2. 사용자 역할을 "USER"로 선택
3. 라이센스 타입을 "FREE"로 선택
4. 라우트 접근 권한 표에서 결과 확인:
   - ✅ 기본 라이센스 관련 페이지들 접근 가능
   - ❌ 관리자 페이지들 접근 불가
   - ❌ 프리미엄 기능들 접근 불가

### 시나리오 2: 관리자 권한 테스트

1. 사용자 역할을 "ADMIN"으로 변경
2. 라이센스를 "ENTERPRISE"로 변경
3. 결과 확인:
   - ✅ 대부분 페이지 접근 가능
   - ✅ 관리자 전용 페이지 접근 가능
   - ❌ SUPER_ADMIN 전용 페이지만 접근 불가

### 시나리오 3: 무단 접근 테스트

1. 권한이 없는 라우트의 "강제 시도" 버튼 클릭
2. `/unauthorized` 페이지로 리다이렉트 확인
3. 적절한 에러 메시지 및 안내 확인

## 🛠️ 디버깅 활용법

### 1. 권한 로직 검증

- 특정 사용자 설정에서 예상과 다른 결과가 나오는 경우
- "권한 요구사항 상세" 섹션에서 요구사항 확인
- 현재 사용자 정보와 비교 분석

### 2. 네비게이션 메뉴 확인

- 실제 앱에서 보여질 메뉴 구조 미리 확인
- 권한 변경 시 메뉴 항목 변화 확인

### 3. 라이센스 기능 테스트

- 라이센스 업그레이드/다운그레이드 시 기능 접근성 변화
- 기능별 라이센스 요구사항 확인

## ⚠️ 주의사항

1. **테스트 환경 전용**: 이 페이지는 개발/테스트 환경에서만 사용
2. **실제 인증 미연동**: 실제 인증 시스템과는 별도로 동작하는 시뮬레이션
3. **상태 초기화**: 페이지 새로고침 시 테스트 사용자 설정 초기화

## 🔧 개발자를 위한 정보

### 핵심 함수들

```typescript
// 접근 가능한 라우트 필터링
const accessibleRoutes = getAccessibleRoutes(testUser);

// 네비게이션 메뉴 생성
const navigation = generateNavigation(testUser);

// 개별 라우트 권한 체크
const hasAccess = checkRouteAccess('/admin/users', testUser);

// 기본 리다이렉트 경로 결정
const redirect = getDefaultRedirectPath(testUser);
```

### 라우트 메타데이터 구조

```typescript
interface RouteItem {
  path: string;
  requiredRoles?: EUserRole[];
  requiredLicenses?: ELicenseType[];
  requiredFeatures?: string[];
  requireAuth?: boolean;
  requireBoth?: boolean;
  category?: 'auth' | 'dashboard' | 'admin' | 'license' | 'public';
  title?: string;
  description?: string;
}
```

## 📈 개선된 시스템의 장점

1. **단일 설정**: 더 이상 라이센스별로 각각 path JSON 구성 불필요
2. **실시간 테스트**: 권한 변경 즉시 결과 확인 가능
3. **시각적 피드백**: 색상과 아이콘으로 직관적 상태 표시
4. **종합적 검증**: 개별 기능부터 전체 시스템까지 한 번에 테스트

이 테스트 페이지를 통해 새로운 라우팅 시스템이 올바르게 작동하는지 확인하고,
다양한 사용자 시나리오에서의 동작을 검증할 수 있습니다! 🎉
